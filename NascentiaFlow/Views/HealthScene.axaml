<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:NascentiaFlow.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:DataType="vm:HealthSceneModel"
             x:Class="NascentiaFlow.Views.HealthScene">
    <ScrollViewer>
        <StackPanel>
            <DataGrid ItemsSource="{Binding BodyWeights}"
                      IsReadOnly="True"
                      CanUserSortColumns="True"
                      SelectionMode="Single"
                      SelectedItem="{Binding SelectedBodyWeight, Mode=OneWayToSource}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date, Converter={StaticResource InstantToStringConverter}}" />
                    <DataGridTextColumn Header="Weight" Binding="{Binding Weight}" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
